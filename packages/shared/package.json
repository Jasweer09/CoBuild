{
  "name": "@cobuild/shared",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "check-types": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cobuild/eslint-config": "workspace:*",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "rimraf": "^6.0.0"
  }
}
